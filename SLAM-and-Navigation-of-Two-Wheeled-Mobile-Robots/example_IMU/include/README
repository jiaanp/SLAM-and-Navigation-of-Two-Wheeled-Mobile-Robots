# example_IMU 说明

`example_IMU` 是一个基于 `ESP32 + MPU6050` 的最小 IMU 读取示例，用于验证传感器连线、I2C 通信以及姿态数据读取是否正常。

## 功能概述

当前示例会完成以下工作：

- 初始化串口，波特率为 `115200`
- 使用 `Wire.begin(18, 19)` 初始化 I2C
- 连接并初始化 `MPU6050`
- 上电后执行一次零偏标定
- 周期性输出温度、加速度、角速度和角度信息

对应主程序见：

- `src/main.cpp`

## 硬件连接

本示例默认使用以下 I2C 引脚：

- `SDA -> GPIO18`
- `SCL -> GPIO19`

请同时确认：

- `MPU6050` 供电正常
- `GND` 与 `ESP32` 共地
- 上电标定阶段保持传感器静止

## 依赖库

项目通过 `PlatformIO` 管理依赖，当前依赖配置见：

- `platformio.ini`

主要库为：

- `MPU6050_light`

## 编译与烧录

在 `example_IMU` 目录下执行：

```bash
pio run
pio run -t upload
```

如果本机使用串口监视器查看输出，可执行：

```bash
pio device monitor -b 115200
```

## 串口输出说明

程序启动后会先输出初始化状态，例如：

```text
MPU6050 status: 0
Calculating offsets, do not move MPU6050
Done!
```

其中：

- `status = 0` 表示 IMU 初始化成功
- 标定阶段请不要移动传感器

随后程序会每秒输出一次：

- 温度 `TEMPERATURE`
- 加速度 `ACCELERO`
- 角速度 `GYRO`
- 加速度解算角 `ACC ANGLE`
- 姿态角 `ANGLE`

## 代码行为说明

程序核心流程如下：

1. 在 `setup()` 中初始化串口和 I2C
2. 调用 `mpu.begin()` 检查 MPU6050 是否可通信
3. 调用 `mpu.calcOffsets(true, true)` 进行陀螺仪和加速度计标定
4. 在 `loop()` 中持续执行 `mpu.update()`
5. 每隔 1 秒打印一次当前 IMU 数据

## 适用场景

这个示例适合用于：

- 验证 MPU6050 是否接线正确
- 检查 I2C 引脚配置是否正确
- 初步观察姿态变化是否正常
- 为后续接入 micro-ROS / `sensor_msgs/Imu` 发布做前置验证

## 常见问题

### 1. 串口一直停在 `MPU6050 status`

通常说明：

- IMU 未正确连接
- I2C 引脚配置不对
- 供电异常

### 2. 角度数据漂移

MPU6050 本身会有零偏和积分漂移，这是正常现象。该示例主要用于基础验证，不包含更高阶的姿态融合或漂移补偿逻辑。

### 3. 数据没有变化

请确认：

- `mpu.update()` 被持续调用
- 传感器确实在移动
- 串口波特率设置为 `115200`

## 后续扩展

如果需要把这个示例接入机器人系统，常见扩展方向包括：

- 发布 `sensor_msgs/Imu`
- 与编码器里程计进行 `EKF` 融合
- 将原始数据转换为 ROS 标准单位
- 增加姿态四元数输出
